#import "./Post.graphql"
#import "./Comment.graphql"

mutation addPost($input: AddPostInput!, $file: FileUpload!) {
  addPost(input: $input, file: $file) {
    ...PostInfo
    comments {
      ...CommentInfo
    }
  }
}
